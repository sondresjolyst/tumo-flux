apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: pyttogpanne-api-health
  namespace: pyttogpanne-dev
spec:
  # The probes reach the pod directly, so nothing needs to read /health through the ingress.
  ipAllowList:
    sourceRange:
      - 127.0.0.1/32
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pyttogpanne-api-health
  namespace: pyttogpanne-dev
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`pyttogpanne-api-dev.prod.tumogroup.com`) && PathPrefix(`/health`)
    # Above the router the chart's Ingress creates for the same host at /.
    priority: 100
    middlewares:
    - name: pyttogpanne-api-health
    services:
    - name: pyttogpanne-api
      port: 80
  tls:
    secretName: pyttogpanne-api-tls
