apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - role.yaml
  - vault-transit-bootstrap-job.yaml
  - vault-main-bootstrap-job.yaml
configMapGenerator:
  - name: vault-bootstrap-script
    namespace: vault-transit
    files:
      - vault-bootstrap.sh=vault-bootstrap-script.sh
